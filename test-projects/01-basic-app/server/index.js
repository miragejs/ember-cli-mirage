/* eslint-env node */
'use strict';

// const myMirageMiddleware = require('ember-cli-mirage').makeMiragMiddleware();

module.exports = function(app) {
  // app.use(myMirageMiddleware);

  // app.get('/ember-hamsters', function(req, res) {
  //   res.json({
  //     hello: 'world',
  //     some: 'foo'
  //   });
  // });
};
